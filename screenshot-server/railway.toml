[build]
builder = "NIXPACKS"

[deploy]
startCommand = "npx playwright install chromium --with-deps && node index.js"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3

[[services]]
name = "screenshot-server"
